---
name: write-tests
description: How to write and run tests here, and what is worth testing at all. Use whenever you add or change a function under src/lib or src/access, when pnpm test reports a module that has no test, when you are about to write a comment explaining why some code must not change, or when you need to confirm a fix actually fixed something.
---

**Read `.claude/skills/write-tests/SKILL.md` and follow it.** That file holds the actual guidance and
is written tool-agnostically, so ignore its frontmatter and treat every rule in it as if it were
written here.

This file exists only so Grok Code can see the trigger above. It is generated: run `pnpm sync:skills`
after editing the source, and never edit this copy by hand.
