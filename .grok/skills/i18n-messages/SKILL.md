---
name: i18n-messages
description: Why a translated string can render as a raw key path in the browser, and what to do about it. Use whenever you add or rename a key in messages/pl.json or messages/en.json, make a component "use client" that reads translations, or see a literal key path like Blog.title rendered on the page.
---

**Read `.claude/skills/i18n-messages/SKILL.md` and follow it.** That file holds the actual guidance and
is written tool-agnostically, so ignore its frontmatter and treat every rule in it as if it were
written here.

This file exists only so Grok Code can see the trigger above. It is generated: run `pnpm sync:skills`
after editing the source, and never edit this copy by hand.
