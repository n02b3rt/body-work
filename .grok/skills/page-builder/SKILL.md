---
name: page-builder
description: The constraints that make the page builder's canvas and the public site render identically. Use whenever you add or change an element under src/lib/builder/elements/registry.ts, src/components/builder/render/, src/components/builder/editor/, or src/components/builder/inspector/, or when a builder element renders differently in the editor canvas than on the site.
---

**Read `.claude/skills/page-builder/SKILL.md` and follow it.** That file holds the actual guidance and
is written tool-agnostically, so ignore its frontmatter and treat every rule in it as if it were
written here.

This file exists only so Grok Code can see the trigger above. It is generated: run `pnpm sync:skills`
after editing the source, and never edit this copy by hand.
