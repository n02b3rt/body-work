---
name: new-route
description: The checklist for adding a page or route so it renders statically, ships metadata, and appears in the sitemap. Use whenever you create a new page.tsx or route.ts under src/app, split an existing route, or find that a page is rendering dynamically, is missing a title, or is absent from the sitemap.
---

**Read `.claude/skills/new-route/SKILL.md` and follow it.** That file holds the actual guidance and
is written tool-agnostically, so ignore its frontmatter and treat every rule in it as if it were
written here.

This file exists only so Grok Code can see the trigger above. It is generated: run `pnpm sync:skills`
after editing the source, and never edit this copy by hand.
